[workspace]
resolver = "2"
members = ["enclave_kernel", "aerospace_payload"]
default-members = ["enclave_kernel"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = "z"

[profile.release.package."*"]
opt-level = "z"
